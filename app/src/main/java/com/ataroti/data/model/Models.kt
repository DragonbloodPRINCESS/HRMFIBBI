package com.ataroti.data.model

enum class Mode { LIVE_ORACLE, SANDBOX }
enum class Orientation { UPRIGHT, REVERSED, ROT90, ROT270 }
enum class CardKind { MAJOR, WANDS, CUPS, SWORDS, PENTACLES }
enum class ChaosLevel { MINIMAL, MODERATE, WILD }
enum class EventType { JUMP_OUT_PAIR, DOUBLE_PULL, SHADOW_UNDER }
